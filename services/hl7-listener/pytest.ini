# services/hl7-listener/pytest.ini
[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short -q
markers =
    integration: Tests requiring external services (Pub/Sub emulator, real DB). Skipped by default.
