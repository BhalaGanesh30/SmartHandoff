[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: Integration tests (require emulator)
    unit: Unit tests (no external dependencies)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
