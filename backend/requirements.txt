alembic>=1.13.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
cryptography>=42.0.0
google-cloud-secret-manager>=2.20.0
google-cloud-pubsub>=2.18.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
email-validator>=2.1.0

# --- Authentication & JWT (US-056) ---
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0
cachetools>=5.3.0
authlib>=1.3.0
httpx>=0.27.0
bcrypt>=4.0.0

# --- RBAC (US-057) ---
PyYAML>=6.0.1

# --- Redis / JWT blocklist (US-059) ---
redis[hiredis]>=5.0.0

# --- Twilio Verify OTP (US-065) ---
twilio>=9.0.0

# --- FHIR R4 resource models (US-017) ---
fhir.resources>=7.1.0

# --- AI/ML — LangChain + Vertex AI (US-025) ---
langchain-google-vertexai>=2.0.0
langchain-core>=0.3.0

# --- Patient Instructions & Reading Level (US-027) ---
textstat>=0.7.3
sentence-transformers>=2.7.0

# --- Prometheus metrics (US-018) ---
prometheus_client>=0.20.0
