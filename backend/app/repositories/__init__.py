"""Repository layer for database queries.

Repositories encapsulate read queries and provide a clean interface between
the API layer and the database models. Write operations are typically handled
directly in service layers or agent tasks.

US-030: Medication reconciliation query layer.
"""
